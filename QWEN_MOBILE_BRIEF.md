# MindGlass 移动端适配 Brief

## 任务
给 MindGlass 前端做移动端响应式适配，让手机端也能正常使用。

## 要求
1. **使用 Tailwind CSS** 做响应式布局
2. **不影响当前桌面端效果** — 桌面端保持原样，手机端新增适配
3. **双端可用** — 同一套代码，PC 和手机都能正常显示和操作
4. **渐进增强** — 手机端优先保证核心功能可用（对话、推理图展示、干预操作）

## 重点适配
- 对话面板（ChatPanel）：手机端全宽，气泡自适应
- 推理图（ReasoningGraph）：手机端可缩放/拖拽，节点文字缩小
- 水镜空状态（WaterMirrorEmpty）：手机端居中显示
- 侧边栏/工具栏：手机端折叠或底部导航
- 字体大小：手机端适当放大，保证可读

## 技术要点
- 安装 Tailwind CSS（如果还没装）
- 在现有 Vue 组件中用 Tailwind 的响应式类（sm:, md:, lg:）
- 不破坏现有样式，用 `@apply` 或新增 class 覆盖
- 测试：Chrome DevTools 手机模拟器 + 真实手机访问

## 验收标准
- [ ] 手机端能正常打开页面
- [ ] 对话功能正常（发送、接收、流式显示）
- [ ] 推理图能查看和操作（缩放/拖拽）
- [ ] 桌面端效果不变
- [ ] 代码在 `feature/mobile-responsive` 分支

## 进度
每完成一个模块，更新 `PROGRESS.md`，记录：
- 完成了什么
- 改了哪些文件
- 遇到的问题

## 备注
- 不用急，慢慢做
- 做完后读 notes 验收
- 有问题随时问（但尽量自己解决）
