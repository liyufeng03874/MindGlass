# MindGlass 移动端适配任务

## 目标
让 MindGlass 前端支持手机端访问，使用 Tailwind CSS 做响应式布局，适配双端（PC + 手机）。

## 背景
- 当前 MindGlass 只有 PC 端布局，手机访问体验很差
- 用户反馈：不在电脑旁时很难用
- 哥哥自己也会用手机访问

## 要求
1. **使用 Tailwind CSS** 做响应式布局
2. **不影响当前 PC 端效果** — PC 端保持原样，手机端新增适配
3. **渐进式开发** — 不用一次性做完，可以分模块做：
   - 先做主聊天界面（ChatPanel）
   - 再做推理图（ReasoningGraph）
   - 最后做管理后台（AdminView）
4. **开新分支** — `feature/mobile-responsive`（已开）

## 验收标准
- 手机端能正常访问、聊天、看推理图
- PC 端效果不受影响
- 响应式断点合理（手机 < 768px，PC >= 768px）

## 进展记录
每完成一个模块，更新此文件。

## 注意
- 不用一直盯，慢慢做
- 做完一个模块就 commit
- 有问题写 notes，不要卡住
