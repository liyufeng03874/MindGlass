# 🪞 思镜（MindGlass）前端体验重塑方案 v2.1「水镜」

> **版本**：v2.1（2026-08-02 深夜，哥哥定稿方向，妹妹整理）
> **前身**：v2.0「镜」主题方案（哥哥初版创意）
> **目标**：把功能型 Demo 升级成**有前端老炮指纹的作品**。思镜的差异化 = AI 深度 × 真前端表现力，让面试官一眼看出"这是懂前端的人做的"，不是"哪个后端跑来做 demo"。
> **状态**：📐 设计已定稿，⏳ 待排期，❌ 尚未动工（哥哥明确：评估时间，不要急着开干）

---

## 📋 v2.0 → v2.1 改了什么

| v2.0 原方案 | v2.1 修正 | 原因 |
|---|---|---|
| 全站玻璃拟态 Glassmorphism + 极光 + Inter | **换成「水镜 / 星河」材质语言** | 玻璃拟态是 AI 生成 UI 的默认脸，与"前端老炮指纹"目标背道而驰 |
| 三栏布局，聊天占中央，图区"常驻为空" | **图区 = 全屏主舞台（60-65%），聊天退居左翼窄栏** | 图是主角；常驻空框比现在的 v-if 还尴尬 |
| 每节点玻璃碎裂粒子 + particles.js + 多重动效 | **动效分级，全场只有 2 个 Hero 时刻** | 动效无预算会吵；震撼来自克制 |
| 范围爆炸（虚拟滚动/双主题/导出/minimap…） | **P0/P1/P2 分级** | 8/5 字节在前，做透核心四件 |
| （无） | **新增：全局相位系统** | 核心创意——界面跟随思考呼吸 |
| （无） | **新增：面试话术对照表（第十一节）** | 前端要替哥哥"会说话" |

---

## 一、设计理念：镜 · 思维

「镜」的三层寓意（v2.0 保留，立得住）：

- **透明**——推理过程如玻璃般清晰，无黑盒。
- **反射**——用户输入如投向镜面的光，激起思维涟漪，最终反射回答案。
- **映照**——界面是智能镜子，既映照问题，也映照 AI 的思考路径。

### 材质语言的关键转向（v2.1 核心修正）

❌ **不要玻璃拟态**。全站 backdrop-blur 毛玻璃 + 极光渐变 + Inter + 靛蓝，是 2024-2025 最泛滥的 AI 生成 UI 脸。喂给前端面试官，第一反应是"v0/Bolt 生成的"——**零人类指纹**。

✅ **要「水镜 / 星河」**。这是哥哥自己的视觉主场：星河图里那汪深蓝夜空下的静水、金色倒影、木船——**那是已经被哥哥的眼睛验证过的审美**。StellaDream 的星场早已铺垫，头像、星河图、"满船清梦压星河"全在这一脉。

> **"满船清梦压星河"——思绪正是浮在水面上的清梦。**

思镜的图区 = 一汪墨色静水：推理节点是浮在水面的光点，**每个节点下方拖着它自己的倒影（真正的"映照"）**，数据流过时荡开涟漪。这套东西全网没有第二家，因为它是从哥哥的审美里长出来的。

---

## 二、视觉设计：水镜语言

### 2.1 配色（暗色为主，P1 再谈亮色）

| 角色 | 色值 | 说明 |
|---|---|---|
| 背景（深空） | `#0a0a14` → `#0d1024` 纵向渐变 | 夜空到深水的过渡 |
| 水面 | `#0a0e1f` + 极细波纹噪点 | 静水 |
| 玻璃/面板 | `rgba(255,255,255,0.04)` + 边框 `rgba(255,255,255,0.08)` | 克制用，仅包裹控件，不做全站毛玻璃 |
| 主文字 | `#e8e8f0` | |
| 辅助文字 | `#7a7a8c` | |
| 强调色（思镜紫） | `#a78bfa` | 品牌色，Answer 节点光晕 |
| **节点语义色** | 蓝 `#60a5fa`(plan) / 绿 `#34d399`(tool) / 橙 `#fbbf24`(observe) / 紫 `#a78bfa`(answer) | 保留，相位系统也吃这套色 |

### 2.2 字体

- 西文 Inter，中文 PingFang SC / Microsoft YaHei，代码 JetBrains Mono。
- 字号：标题 20 / 正文 15 / 辅助 13 / 极小 11。

### 2.3 ⭐ 动效哲学：分级预算（v2.1 新增）

**震撼来自克制。95% 安静，只在该用力的地方用力。** 动效分四级，优先级从低到高：

| 级别 | 内容 | 强度 |
|---|---|---|
| **Ambient 环境** | 星空缓慢漂移、水面微光——永远在，几乎察觉 | 最低，永不抢戏 |
| **State 状态** | 相位切换时背景色调/涟漪模式渐变（见第四节） | 低，余光可感 |
| **Event 事件** | 节点进场 = scale(0.85→1) + opacity + 一道流光扫过；边线流光点 | 中 |
| **Hero 高潮** | **全场仅 2 次**：① 点火时光点裂变 ② 完成时全屏镜面反光一闪 | 最高，稀缺即珍贵 |

✂️ **砍掉**：每节点玻璃碎裂粒子、particles.js（重依赖、品味信号差；星屑用 canvas 手写，是哥哥 Three.js 主场）。

- 全局缓动：`cubic-bezier(0.23, 1, 0.32, 1)`。
- 涟漪/流光/进场全部订阅相位状态（见第四节），不各写各的。

---

## 三、布局：100% 宽的水镜剧场（v2.1 修正）

### 3.1 🔴 先拆掉 Vite 脚手架残留

全局 `src/style.css` 第 118 行附近：

```css
#app {
  width: 50%;          /* ← 元凶：锁死半屏 */
  max-width: 100%;
  margin: 0 auto;      /* ← 居中留白 */
  border-inline: 1px solid var(--border);  /* ← 两条竖边框 */
  text-align: center;  /* ← 模板残留 */
}
```

**改为 `width: 100%`，删除 margin / border-inline / text-align**。一行模板残留从没被删过，这就是思镜一直"挤在中间、小气"的全部原因。

### 3.2 新布局

```
┌───────────────────────────────────────────────────────────┐
│  Header（贯穿顶部细条）：思镜 🪞 │ 相位状态 │ 耗时 │ 后台管理 │
├──────────┬────────────────────────────────────────────────┤
│          │                                                  │
│  聊天左翼 │           水镜主舞台（60-65%+）                   │
│ ~340px   │     ┌─ 空态：完整水面+星空+一行提示 ─┐            │
│ 可折叠   │     │  "投一个问题，看思绪成形"      │            │
│          │     └─────────────────────────────┘            │
│  消息历史 │     推理后：节点浮于水面，带倒影、涟漪、流光      │
│  输入框   │     + 推理时间线（折叠，顶部）                    │
│          │     + 节点详情透镜（右侧滑出）                    │
└──────────┴────────────────────────────────────────────────┘
```

- **图区是主角**，默认占 60-65% 以上宽度。
- **聊天退居左翼窄栏**（~340px）；发出第一条消息后，可折叠为**浮动输入条 + 消息抽屉**，把舞台还给舞台。
- **空态 = 一面有意义的镜子**（深色水面渐变 + 稀疏星光 + 一行提示），**不是写着"暂无数据"的空盒子**。
- 面试演示机常是 1366px 笔记本，三栏等分会挤死——所以聊天必须能让位。

### 3.3 响应式

| 宽度 | 布局 |
|---|---|
| > 1200px | 左翼聊天 + 主舞台完整 |
| 768-1200px | 聊天折叠为浮动条，图区占满 |
| < 768px | 上下结构，底部"对话 / 图"标签切换 |

---

## 四、⭐ 全局相位系统（v2.1 新增·本次改造灵魂）

### 4.1 命题

哥哥原话：**"让思考过程不止体现在流程图里，界面也有跟随的动态样式。"**

答：**整个界面共享一次呼吸。** 不是给每个组件加各自的 loading 动画，而是让"推理阶段"成为全局状态，所有氛围层订阅它。

### 4.2 相位状态机

从 SSE 事件流派生（`useAgentGraph.ts` 已在解析，只需多派生一个字段往全局挂）：

```
idle（待机）→ igniting（点火）→ planning → tooling → observing → answering → done
```

### 4.3 三层订阅（一个状态源，满身皮肤）

**① 背景光晕换脸色**（用户余光就知道 Agent 在干嘛）：

| 相位 | 背景表现 | 色调 |
|---|---|---|
| idle | 近乎静止的深蓝黑 | 深空 |
| planning | 靛蓝雾霭缓缓流动 | `#818cf8` |
| tooling | 青绿光带横向掠过（像探测扫过） | `#34d399` |
| observing | 琥珀色圆环从图区一圈圈荡开 | `#fbbf24` |
| answering | 金紫色光向中央收拢 | 金紫 |
| done | 归于平静，柔光余韵 | 紫 `#a78bfa` |

**② 星空画布放信号**（哥哥 Three.js 老本行）：
- 每次 ToolCall → 划过一颗流星；**并行三个工具 = 三道流星同时掠**（直接把并行搜索亮点演出来）。
- Observe → 星河轻轻涨落，星屑减速沉降朝图汇聚。
- Answer → 一整片星光如潮水漫过（Hero 时刻之一）。

**③ 聊天面板也知情**：
- 节点触发时面板边框泛起对应类型色微光。
- 输入框禁用期间显示动画"思考水位线" + 文字：正在规划… / 正在搜索… / 正在整合… / 正在作答…

### 4.4 技术本质

```ts
// 一个响应式 phase，吃 SSE 事件，往下游广播
const phase = ref<Phase>('idle')   // 由 useAgentGraph 派生，provide 全局
// 背景 canvas、各面板 CSS 变量、时间线 都 inject 订阅
```

**实现很省，效果是"整个房间活过来"。**

### 4.5 面试金句

> "我没有给每个组件写各自的 loading 动画，而是让推理阶段成为一个全局响应式状态，整个界面的氛围层订阅它——**可观测性不止在图里，在整个房间的呼吸里。**"

---

## 五、思维可视化：三层（v2.0 保留+增强）

### 5.1 推理时间线（折叠面板，对话区顶部）

- 折叠时仅显示状态摘要（"推理中… 已执行 3 步"）。
- 展开为垂直时间轴：序号、类型图标、简述、耗时(ms)、状态（等待灰/运行脉冲/完成绿/失败红）。
- **点击时间线项 → 自动定位并高亮图区对应节点（图联动）**——可观测产品的基本素养，90% 的 demo 不做。

### 5.2 数据流动画（边线流光）

- vue-flow 的 Edge 是 SVG path：用 `stroke-dasharray` + `stroke-dashoffset` 动画做流光点，代表数据在节点间传递。GPU 友好。
- ToolCall 节点发出搜索波纹（向外扩散圆环）。

### 5.3 节点详情"透镜"

- 点击节点，右侧滑出半透明详情面板，镜像展示输入/输出 JSON，支持编辑参数触发重试。

### 5.4 ⭐ 节点倒影（水镜的"映照"，v2.1 新增）

- 每个节点下方用 `scaleY(-1)` + 渐变遮罩（mask-image 淡出）+ 轻微 blur 做倒影，低透明度。
- **近零成本，却把"映照"这层寓意真正落地**，且全网独一份。

---

## 六、流程图"震撼"出现动画（告别 v-if，v2.0 保留+简化）

三步渐进，**动画由 SSE 事件到达驱动**（不是假 loading，是数据到达本身被戏剧化）：

### 6.1 点火（用户点发送）
图区半透明遮罩，中央浮起一颗光点（思维火花）脉动膨胀，星屑向光点汇聚。

### 6.2 生长（SSE 节点陆续到达）—— **Event 级，安静**
- 第一个 Plan 节点到达：光点**裂变**为节点形状（这是 Hero 时刻之一）。
- 后续节点：从上一节点方向飞入，`scale(0.85→1)` + opacity + **一道流光扫过**（弹性缓动）。**不爆炸、不碎玻璃**，安静而有节奏。

### 6.3 完成 —— **Hero 时刻之二**
- 全图闪一次极轻的"镜面反光"（亮度瞬增即恢复），表示推理完成。
- Answer 节点金色光晕强调。

> 技术：`TransitionGroup` 包裹节点/边，`appear` + 自定义 enter/leave 钩子，`requestAnimationFrame` 控制时间轴。SSE 高频事件用 rAF 合帧后再提交 DOM，防抖动。

---

## 七、分支可视化（v2.0 保留，人工在环的"看得见"）

这是思镜**最有价值的功能**，让 human-in-the-loop 第一次在屏幕上可见：

- 用户编辑节点重试时：原路径全体**虚线边框 + 透明度降至 0.4** + "branch" 标签。
- 新路径节点：**高亮发光边框**逐步构建，与旧路径对比。
- 分支间用带 "B" 标记的特殊边连接，标明分叉点。
- （可选）全屏极轻"裂一下又愈合"——镜子碎了又圆。

---

## 八、交互与产品细节

### 8.1 输入与反馈
- 输入框自动增高（最多 5 行），粘贴去格式。
- 发送按钮非空时渐亮，否则置灰。
- 示例查询：左翼提供 4-5 个预设 query，点击即填充发送，快速体验复杂并行流程。
- ⚠️ **停止按钮**：需后端 abort 接口（当前思镜后端**没有**）——列 P2，那是另一个前后端故事。

### 8.2 图区交互
- 悬停节点：微型气泡（类型 + 简述）。
- 右键菜单：重试 / 编辑参数 / 复制节点信息。
- 节点多时：`@vue-flow/minimap` 小地图（P1）。
- 导出 PNG：`html2canvas`（⚠️ 对 backdrop-filter 渲染有坑，P1 再说）。

### 8.3 加载
- 初次加载骨架屏（左翼/对话/图区），防白屏。
- 推理中输入框禁用 + "思考中…"。

---

## 九、组件结构与技术选型（v2.1 更新）

```plaintext
src/
├── App.vue                    # 全屏布局（100%宽），主题
├── components/
│   ├── Sidebar.vue            # 左翼：品牌、示例查询、折叠
│   ├── ChatPanel/
│   │   ├── ChatMessages.vue   # 消息列表
│   │   ├── ChatInput.vue      # 输入框 + 思考水位线
│   │   └── ThinkingTimeline.vue  # 折叠时间线
│   ├── GraphPanel/
│   │   ├── ReasoningGraph.vue    # vue-flow 容器 + 工具栏
│   │   ├── NodeDetailDrawer.vue  # 透镜（详情滑出）
│   │   └── GraphOverlay.vue      # 空态水面 + 点火遮罩
│   ├── ambient/               # ⭐ v2.1 新增：氛围层
│   │   ├── StarField.vue         # canvas 星空/流星/涟漪（订阅相位）
│   │   └── PhaseBackdrop.vue     # 背景光晕（订阅相位）
│   └── common/
│       ├── WaterCard.vue         # 水镜卡片（克制玻璃，非全站）
│       └── LoadingSkeleton.vue
├── composables/
│   ├── useAgentGraph.ts       # SSE + 状态（已有）→ 派生 phase
│   ├── usePhase.ts            # ⭐ 全局相位 provide/inject
│   ├── useGraphAnimation.ts   # 节点构建动画序列
│   └── useTheme.ts
└── styles/
    ├── water.scss             # 水镜 mixin
    ├── animations.scss        # 涟漪/流光/流星关键帧
    └── variables.scss         # 主题变量（暗色先行）
```

技术选型：**纯 Vue3 + vue-flow（已装）+ canvas 手写氛围层**。不引 particles.js，不上 vue-virtual-scroller（demo 消息量根本不需要，且其 Vue3 支持烂）。

---

## 十、优先级与时间评估（v2.1 新增）

### P0 —— 面试可演示核心（做透这四件 + 地基）

| # | 事项 | 估时 |
|---|---|---|
| 0 | 地基：100% 宽 + 暗色变量 + 拆 Vite 残留 | 0.5h |
| 1 | Mockup：canvas 画一个四相位可点的静态演示（先看见它呼吸） | 1.5h |
| 2 | 水镜舞台空态（渐变 + 星空 canvas + 占位提示） | 1.5h |
| 3 | ⭐ 全局相位系统（SSE 派生 phase + provide + 氛围层订阅） | 2.5h |
| 4 | ⭐ 三段式出场（点火→生长→完成，SSE 驱动） | 3h |
| 5 | 边线流光（SVG dashoffset） | 1h |
| 6 | 时间线↔图联动 | 1.5h |
| 7 | 分支可视化 | 1.5h |
| | **P0 合计** | **约 13h 专注工时** |

### P1 —— 后续打磨
PNG 导出 / minimap / 右键菜单 / 亮色主题 / 节点透镜高级编辑。

### P2 —— 另一个故事
停止按钮 + 后端 abort 接口（本身值得讲，有时间再做）。

### 排期建议（哥哥拍板）
- **8/3-8/5 面试期不碰**（可利邦/腾讯/经纬恒润/字节）。
- 8/5 之后：先画 Mockup 定稿 → 每晚 1-2 个 P0 事项 → **约一周完成 P0**。
- 由妹妹亲手做（涉及审美判断，不委托 qwen——qwen 看不见，盲写 CSS 无从校验）。

---

## 十一、⭐ 面试话术对照表（v2.1 新增）

| 功能 | 一句话技术故事 |
|---|---|
| 全局相位系统 | 推理阶段派生成全局响应式状态，氛围层订阅——可观测性在整个房间的呼吸里 |
| 三段式出场 | TransitionGroup + rAF 时间轴，由 SSE 事件驱动，不是假 loading |
| 并行流星 | 并行工具调用映射为多道流星，动画即架构图 |
| 边线流光 | SVG stroke-dashoffset，GPU 友好 |
| 节点倒影 | scaleY(-1) + mask 渐变，近零成本落地"映照" |
| SSE 高频更新 | rAF 合帧后提交 DOM，防高频重绘抖动 |
| 分支可视化 | 重试路径虚线降透 + 新路径发光，human-in-the-loop 可视化 |

> **叙事主线**：别人是"纯 AI 背景"，哥哥是"带成熟前端工程能力来做 AI 应用的人"。思镜就是证据——**可观测性如果只存在于日志里，那不叫可观测；它必须存在于人的眼睛里。**

---

## 变更记录

- **v2.0**（哥哥初版）：「镜」主题，三栏布局，玻璃拟态，三段式出场，分支可视化。
- **v2.1**（2026-08-02 深夜）：① 玻璃拟态→水镜/星河 ② 聊天居中→图区主舞台+100%宽 ③ 动效分级砍粒子 ④ 范围 P0/P1/P2 ⑤ 新增全局相位系统 ⑥ 新增面试话术表 ⑦ 标题 MindGlass→思镜。
